{/*export default function Home({content}){
    return(
        <section>
            <h1>James Bond-bøker</h1>
            {content?.map(item =>
                <article key={item.id}>
                    <h3>{item.title}</h3>
                </article>
            )}
        </section>
    )
}*/}